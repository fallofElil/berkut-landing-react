import React, { FC } from 'react';

import Particles from "../components/particles/particles";

const App: FC = () => {
  return (
    <div>
      <Particles />
    </div>
  );
}

export default App;
